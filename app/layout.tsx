import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Premier Hub — Live Premier League Dashboard",
  description: "Real-time Premier League standings, top scorers, fixtures, and squad details. Your ultimate matchday command center.",
  keywords: ["Premier League", "Football", "Soccer", "Standings", "Live Scores"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
